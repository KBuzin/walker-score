import React from 'react'
import ReactDOM from 'react-dom/client'
import WalkerScore from './WalkerScore.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WalkerScore />
  </React.StrictMode>,
)
